com_position_controller:
    control_mode: {
        type: int,
        default_value: -1,
        description: "Output mode of the CoM controller. Can be one of [0 - velocity, 1- acceleration].",
        validation: {
            one_of<>: [[-1, 0, 1]]
        }
    }
    wbc_name: {
        type: string,
        default_value: "",
        description: "Name of the WBC controller.",
        validation: {
            not_empty<>: null,
        }
    }
    task_name: {
        type: string,
        default_value: "",
        description: "Name of the WBC task this controller is assigned to.",
        validation: {
            not_empty<>: null,
        }
    }
    p_gain: {
        type: double_array,
        default_value: [],
        description: "P-Gain of the controller, size has to be 3.",
        validation: {
            fixed_size<>: [3],
        }
    }
    d_gain: {
        type: double_array,
        default_value: [],
        description: "D-Gain of the controller, size has to be 3.",
        validation: {
            fixed_size<>: [3],
        }
    }
    max_control_output: {
        type: double_array,
        default_value: [],
        description: "Controller output saturation per element. Size has to be 3.",
        validation: {
            fixed_size<>: [3],
        }
    }
